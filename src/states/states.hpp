// states.hpp
// enum of all game states. Includes all states as well for easy includes
#pragma once

enum states
    {
        GAME_STATE,
        MENU_STATE,
        PAUSE_MENU_STATE,
        GAMEPLAY_OPTION_STATE
    };